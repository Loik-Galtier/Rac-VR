using UnityEngine;
using UnityEngine.SceneManagement;
using UnityEngine.UI;

public class SceneLoader : MonoBehaviour
{
    /*
     public string sceneNameToLoad; // Name of the scene to load

     private Button button; // Reference to the UI button

     private void Start()
     {
         // Find the button component on the GameObject
         button = GetComponent<Button>();

         // Check if the button component was found
         if (button != null)
         {
             // Attach a click event listener to the button
             button.onClick.AddListener(LoadScene);
         }
         else
         {
             Debug.LogError("Button component not found on GameObject.");
         }
     }

     private void LoadScene()
     {
         // Check if a valid scene name is provided
         if (!string.IsNullOrEmpty(sceneNameToLoad))
         {
             // Load the scene by name
             SceneManager.LoadScene(sceneNameToLoad);
         }
         else
         {
             Debug.LogError("Scene name to load is not set.");
         }
     }   
    */
}